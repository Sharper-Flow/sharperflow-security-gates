Corrected the shipped Sharperflow CI Standard so its release guidance matches the org's chosen direction. The prior `fixRulesetReleaseBypass` change had documented the GitHub App bypass as the *preferred* way to release onto a ruleset-protected `main`; the org instead decided to *adhere to the ruleset, not weaken it* and adopted tag-only releases (pokeedge #273, web #93). This change rewrites `docs/ci-standard.md`'s release section (renamed '### Release automation') to make **tag-only the default/required** approach — semantic-release pushes only a tag (not governed by the branch ruleset), the ruleset stays strict (`bypass_actors: []`), and the version bump is carried to staging — with the GitHub App bypass demoted to a clearly-labeled escape hatch for repos that must push release commits to `main`. `scripts/apply-ruleset.sh` help text and the refuse-without-bypass guard were reframed so `--no-release-bypass` reads as the normal tag-only path rather than a dangerous override (flags and logic unchanged — the bypass capability and the explicit-choice guard are retained). The README pointer, conformance-checklist items, and the admin-enforcement bullet were aligned. Docs/messaging only — no behavior, flag, or ruleset-JSON changes. Verified: shellcheck + actionlint pass, the guard still exits 2 without an explicit choice, all bypass flags + injection logic intact, and no 'preferred/recommended bypass' language remains. Contract 8/8.