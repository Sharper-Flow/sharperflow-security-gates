Closed a discovery gap in the shipped Sharperflow CI Standard (Change A): the org ruleset's `bypass_actors: []` plus require-PR + required-`Sharperflow CI Gate` would have broken semantic-release, which pushes `chore(release): <v> [skip ci]` commits directly to `main` on PokeEdge and PokeEdge-Web. Fix: `scripts/apply-ruleset.sh` now composes a release bypass actor at apply time (`--bypass-app-id`/`RELEASE_BYPASS_APP_ID` → `{actor_type:Integration, bypass_mode:always}`, with `--bypass-team-id`/`--bypass-user-id` cloud fallbacks) and refuses to apply without one unless `--no-release-bypass` is passed — preventing a release-breaking ruleset. The committed ruleset JSON stays `bypass_actors: []` (no org-specific id committed); the real id is injected only at apply time. `ci-standard.md` gained a "Release automation & ruleset bypass" section recommending a dedicated GitHub App + `actions/create-github-app-token` (App ID not install/client id; `contents:write`; tags unaffected since the ruleset targets `branch`), with a README pointer and conformance-checklist items. Independent research validated the mechanics (bypass skips the entire ruleset; `always` required for direct pushes; App-token pushes attributed to the Integration). Verification: shellcheck, actionlint, ruleset-JSON base assertions, and dry-run behavior (guard exit 2; Integration `always` injection) all pass; reviewer verdict READY; contract 11/11. App creation + `release.yml` wiring remain documented consumer/ops steps; Changes B/C must set the bypass before applying the ruleset.